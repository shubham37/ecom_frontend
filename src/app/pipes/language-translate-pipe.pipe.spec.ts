import { LanguageTranslatePipePipe } from './language-translate-pipe.pipe';

describe('LanguageTranslatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageTranslatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
